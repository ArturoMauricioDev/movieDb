import React from "react";
import { Typography } from "@mui/material";

const Header = () => {
  return (
    <header>
      <Typography variant="h4" component="h1">
        Movies
      </Typography>
    </header>
  );
};

export { Header };
